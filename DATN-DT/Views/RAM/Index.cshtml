@model IEnumerable<DATN_DT.Models.RAM>

@{
    ViewData["Title"] = "Quản lý RAM";
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <title>Quản lý RAM</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        body {
            background-color: #12121d;
            color: white;
            font-family: 'Segoe UI', sans-serif;
        }

        .container {
            margin-top: 50px;
        }

        h2 {
            text-align: center;
            margin-bottom: 30px;
            color: #f5f5f5;
        }

        table {
            background-color: #1e1e2a;
            border-radius: 10px;
            overflow: hidden;
        }

        th {
            background-color: #2b2b33;
            color: #fff;
        }

        td {
            color: #ddd;
        }

        .btn-create {
            background-color: #4a90e2;
            color: white;
            border: none;
            border-radius: 10px;
            padding: 10px 20px;
            transition: 0.3s;
        }

            .btn-create:hover {
                background-color: #357ab8;
            }

        .modal-content {
            background-color: #2b2b33;
            color: white;
            border-radius: 10px;
        }

        .form-control {
            background-color: #1e1e2a;
            color: white;
            border: 1px solid #444;
        }

            .form-control:focus {
                border-color: #4a90e2;
                box-shadow: none;
            }

        .text-danger {
            color: #ff4d4d !important;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Quản Lý RAM</h2>

        <div class="d-flex justify-content-end mb-3">
            <button class="btn btn-create" data-bs-toggle="modal" data-bs-target="#createModal">
                + Thêm RAM
            </button>
        </div>

        <table class="table table-hover text-center align-middle">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Dung lượng RAM</th>
                    <th>Mô tả</th>
                    <th>Hành động</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.IdRAM</td>
                        <td>@item.DungLuongRAM</td>
                        <td>@item.MoTaRAM</td>
                        <td>
                            <button class="btn btn-sm btn-warning btn-edit"
                                    data-id="@item.IdRAM"
                                    data-dungluong="@item.DungLuongRAM"
                                    data-mota="@item.MoTaRAM">
                                Sửa
                            </button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <!-- Create -->
    <div class="modal fade" id="createModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content p-3">
                <div class="modal-header">
                    <h5 class="modal-title">Thêm RAM</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <form id="createForm">
                    <div class="modal-body">
                        <div class="mb-3">
                            <label class="form-label">Dung lượng RAM <span class="text-danger">*</span></label>
                            <input type="text" id="DungLuongRAM" class="form-control" placeholder="Nhập dung lượng RAM" />
                            <small id="errorDungLuongRAM" class="text-danger d-none"></small>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Mô tả</label>
                            <textarea id="MoTaRAM" class="form-control" rows="3" placeholder="Nhập mô tả (nếu có)"></textarea>
                        </div>
                    </div>

                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                        <button type="submit" class="btn btn-primary">Lưu</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!--Edit -->
    <div class="modal fade" id="editModal" tabindex="-1" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content p-3">
                <div class="modal-header">
                    <h5 class="modal-title">Chỉnh sửa RAM</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <form id="editForm">
                    <input type="hidden" id="EditIdRAM" />
                    <div class="modal-body">
                        <div class="mb-3">
                            <label class="form-label">Dung lượng RAM <span class="text-danger">*</span></label>
                            <input type="text" id="EditDungLuongRAM" class="form-control" />
                            <small id="errorEditDungLuongRAM" class="text-danger d-none"></small>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Mô tả</label>
                            <textarea id="EditMoTaRAM" class="form-control" rows="3"></textarea>
                        </div>
                    </div>

                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Hủy</button>
                        <button type="submit" class="btn btn-warning">Cập nhật</button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script>
        // Theeem
        document.getElementById("createForm").addEventListener("submit", async (e) => {
            e.preventDefault();

            const ram = {
                DungLuongRAM: DungLuongRAM.value.trim(),
                MoTaRAM: MoTaRAM.value.trim()
            };

            const response = await fetch('/RAM/Create', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(ram)
            });

            let data = {};
            try { data = await response.json(); } catch { }

            document.getElementById("errorDungLuongRAM").classList.add("d-none");

            if (!response.ok) {
                if (response.status === 400 && typeof data === "object") {
                    if (data.DungLuongRAM) showError("errorDungLuongRAM", data.DungLuongRAM);
                    return;
                }

                if (response.status === 409 && data.message) {
                    showError("errorDungLuongRAM", data.message);
                    return;
                }

                alert(data.message || "Không thể thêm RAM. Vui lòng thử lại!");
                return;
            }

            location.reload();
        });

   
        document.querySelectorAll(".btn-edit").forEach(btn => {
            btn.addEventListener("click", () => {
                EditIdRAM.value = btn.dataset.id;
                EditDungLuongRAM.value = btn.dataset.dungluong;
                EditMoTaRAM.value = btn.dataset.mota;
                new bootstrap.Modal(editModal).show();
            });
        });

        // Update
        document.getElementById("editForm").addEventListener("submit", async (e) => {
            e.preventDefault();

            const id = EditIdRAM.value;
            const ram = {
                IdRAM: id,
                DungLuongRAM: EditDungLuongRAM.value.trim(),
                MoTaRAM: EditMoTaRAM.value.trim()
            };

            document.getElementById("errorEditDungLuongRAM").classList.add("d-none");

            const response = await fetch('/RAM/Edit/' + id, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(ram)
            });

            let data = {};
            try { data = await response.json(); } catch { }

            if (!response.ok) {
                if (response.status === 400 && typeof data === "object") {
                    if (data.DungLuongRAM) showError("errorEditDungLuongRAM", data.DungLuongRAM);
                    return;
                }

                if (response.status === 409 && data.message) {
                    showError("errorEditDungLuongRAM", data.message);
                    return;
                }

                alert(data.message || "Không thể cập nhật RAM. Vui lòng thử lại!");
                return;
            }

            location.reload();
        });

        function showError(id, msg) {
            const el = document.getElementById(id);
            el.textContent = msg;
            el.classList.remove("d-none");
        }

        // Reset form 
        createModal.addEventListener("hidden.bs.modal", () => {
            createForm.reset();
            document.querySelectorAll("#createForm small").forEach(e => e.classList.add("d-none"));
        });

        editModal.addEventListener("hidden.bs.modal", () => {
            editForm.reset();
            document.querySelectorAll("#editForm small").forEach(e => e.classList.add("d-none"));
        });
    </script>
</body>
</html>